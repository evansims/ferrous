# Server Configuration
PORT=3000

# Logging Configuration
RUST_LOG=estuary=debug,tower_http=debug

# Database Configuration
# Options: memory (default), postgres (future), sqlite (future)
DATABASE_TYPE=memory

# Future database connection strings
# DATABASE_URL=postgresql://user:password@localhost/estuary
# DATABASE_URL=sqlite://./estuary.db

# Future authentication configuration (when implemented)
# JWT_SECRET=your-secret-key-here

# CORS configuration (when needed)
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://example.com